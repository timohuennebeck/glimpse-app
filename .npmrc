# Expo SDK 57 pins react@19.2.3, while react-dom@19.3 (pulled in transitively as an
# optional peer of expo + @expo/router-server) demands react@^19.3. Strict peer
# resolution fails on that pre-existing conflict in Expo's own tree.
legacy-peer-deps=true
