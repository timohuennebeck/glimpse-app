<?xml version="1.0" encoding="utf-8"?>
<!--
  2x2 Glimpse widget. Mirrors the iOS SmallWidgetView: the frosted photo fills
  the tile, with the sender, caption and a camera badge along the bottom.

  RemoteViews supports a small fixed set of views, so the frosted treatment is a
  pre-blurred bitmap plus a scrim rather than a runtime blur.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background">

    <FrameLayout
        android:id="@+id/widget_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/widget_photo"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="@string/widget_photo_description" />

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/widget_scrim" />

        <ImageView
            android:id="@+id/widget_lock"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_gravity="center"
            android:src="@drawable/widget_lock_puck"
            android:contentDescription="@string/widget_locked_description" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="horizontal"
            android:gravity="bottom"
            android:padding="9dp">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/widget_sender"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="9sp"
                    android:textColor="#BFFFFFFF" />

                <TextView
                    android:id="@+id/widget_caption"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textSize="11sp"
                    android:maxLines="2"
                    android:ellipsize="end"
                    android:textColor="#FFFFFFFF" />
            </LinearLayout>

            <ImageView
                android:layout_width="27dp"
                android:layout_height="27dp"
                android:src="@drawable/widget_camera_badge"
                android:contentDescription="@string/widget_reply_description" />
        </LinearLayout>
    </FrameLayout>

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:padding="12dp"
        android:visibility="gone"
        android:textSize="12sp"
        android:textColor="#CCFFFFFF"
        android:text="@string/widget_empty" />
</FrameLayout>
